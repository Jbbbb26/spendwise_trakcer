name: spendwise_trakcer
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.11.0

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  
  # Responsive UI tool
  flutter_screenutil: ^5.9.3

  # Firebase & Authentication
  firebase_core: ^3.15.2
  firebase_auth: ^5.4.0
  google_sign_in: ^6.2.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

  # If you have images for your SpendWise UI, uncomment the lines below:
  # assets:
  #   - assets/images/